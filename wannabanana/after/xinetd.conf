service afterimage
{
    disable = no
    type = UNLISTED
    socket_type = stream
    protocol = tcp
    wait = no
    user = ctf
    bind = 0.0.0.0
    port = 10039
    server = /home/ctf/run.sh
    instances = 64
    per_source = UNLIMITED
    cps = 256 10
    flags = REUSE NODELAY
}

