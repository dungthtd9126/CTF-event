version: '3.8'

services:
  challenge:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    restart: unless-stopped
    cpuset: "0-1"
    pids_limit: 1024
    mem_limit: 512m
